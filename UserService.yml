# user-service-dev.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: Soumodip@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081
